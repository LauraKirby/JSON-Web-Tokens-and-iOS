platform :ios, '9.0'

target 'SwiftLogin' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftLogin
  pod "SimpleKeychain"

end
